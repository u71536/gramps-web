# Чеклист миграции: DigitalOcean → OVH (Traefik)

## ✅ Подготовка на старом сервере (DigitalOcean)

- [ ] Подключиться к серверу DigitalOcean
- [ ] Загрузить скрипт `migrate_backup.sh`
- [ ] Выполнить бэкап: `./migrate_backup.sh docker-compose.yml`
- [ ] Скачать архив бэкапа на локальный компьютер
- [ ] Проверить размер и целостность архива

## ✅ Настройка нового сервера OVH

- [ ] Подключиться к серверу OVH
- [ ] Проверить, что Docker установлен: `docker --version`
- [ ] Проверить, что Traefik запущен: `docker ps | grep traefik`
- [ ] Убедиться, что сеть `traefik-network` существует: `docker network ls | grep traefik`
- [ ] Создать рабочую директорию: `sudo mkdir -p /opt/grampsweb && sudo chown $USER:$USER /opt/grampsweb`
- [ ] Загрузить файлы проекта в `/opt/grampsweb`

## ✅ Восстановление данных

- [ ] Загрузить архив бэкапа на сервер OVH
- [ ] Загрузить скрипт `migrate_restore.sh`
- [ ] Выполнить восстановление: `./migrate_restore.sh /path/to/backup.tar.gz`
- [ ] Проверить, что все volumes созданы: `docker volume ls | grep gramps`

## ✅ Конфигурация

- [ ] Использовать готовый `docker-compose.yml` (уже настроен для `familytree.rstak.co`)
- [ ] Или скопировать `docker-compose.traefik.yml.example` и обновить:
  - [ ] Домен в labels (уже: `familytree.rstak.co`)
  - [ ] Имя сети (уже: `traefik-network`)
  - [ ] SMTP настройки (если нужно изменить)
- [ ] Проверить конфигурацию: `docker-compose config`

## ✅ DNS

- [ ] Обновить A запись: `familytree.rstak.co` → IP адрес OVH сервера
- [ ] Подождать распространения DNS (5-30 минут)
- [ ] Проверить DNS: `nslookup familytree.rstak.co`

## ✅ Запуск

- [ ] Перейти в директорию: `cd /opt/grampsweb`
- [ ] Запустить контейнеры: `docker-compose up -d`
- [ ] Проверить статус: `docker-compose ps`
- [ ] Проверить логи: `docker-compose logs -f grampsweb`
- [ ] Проверить, что Traefik видит сервис: `docker logs traefik | grep grampsweb`

## ✅ Проверка работы

- [ ] Открыть https://familytree.rstak.co в браузере
- [ ] Проверить SSL сертификат (зеленый замок)
- [ ] Войти в систему
- [ ] Проверить основные функции:
  - [ ] Просмотр дерева
  - [ ] Поиск
  - [ ] Загрузка медиа (если используется)
  - [ ] Редактирование данных

## ✅ Мониторинг и обслуживание

- [ ] Проверить логи на ошибки: `docker-compose logs`
- [ ] Настроить регулярные бэкапы (см. `MIGRATION_GUIDE_TRAEFIK.md`)
- [ ] Добавить в crontab задачу бэкапа
- [ ] Проверить использование дискового пространства

## ✅ Финальная проверка

- [ ] Оставить старый сервер на несколько дней для проверки
- [ ] Убедиться, что все работает стабильно
- [ ] Уведомить пользователей о миграции (если нужно)
- [ ] После успешной проверки - остановить старый сервер

## 🔧 Устранение проблем

Если что-то не работает:

1. **Traefik не видит контейнер:**
   ```bash
   docker network inspect traefik-network
   docker inspect grampsweb | grep -A 20 Labels
   ```

2. **SSL сертификат не получается:**
   ```bash
   docker logs traefik | grep -i acme
   nslookup familytree.rstak.co
   ```

3. **404 или Bad Gateway:**
   ```bash
   docker exec grampsweb netstat -tlnp | grep 5001
   docker-compose logs grampsweb
   ```

4. **Данные не отображаются:**
   ```bash
   docker volume inspect gramps_db
   docker volume inspect gramps_media
   ```

## 📝 Контакты и ссылки

- Домен: https://familytree.rstak.co/
- Сеть Traefik: `traefik-network`
- Подробное руководство: `MIGRATION_GUIDE_TRAEFIK.md`
- Быстрый старт: `QUICK_START_MIGRATION.md`

